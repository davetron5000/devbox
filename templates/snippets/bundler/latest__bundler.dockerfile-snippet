# dx.source=https://guides.rubygems.org/command-reference/#gem-update
# dx.stability=vendor
RUN echo "gem: --no-document" >> ~/.gemrc && \
    gem update --system && \
    gem install bundler

# vim: ft=Dockerfile
