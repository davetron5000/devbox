# dx.source=https://hub.docker.com/_/postgres
# dx.stability=docker
  postgres:
    image: postgres:15
    pull_policy: "missing"
    environment:
      POSTGRES_PASSWORD: postgres
# vim: ft=yaml nospell
