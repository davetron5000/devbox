Holds per-developer credentials or secrets. These
cannot be symlinks as they must be accessible to 
the Docker container.  This directory should have been
added to .gitignore to prevent it from being checked in.
